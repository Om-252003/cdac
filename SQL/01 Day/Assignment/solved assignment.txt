Q1. SELECT lastname, firstname, jobtitle FROM employees WHERE jobtitle = 'Sales Rep';

SELECT lastname, firstname, jobtitle
FROM employees
WHERE jobtitle = 'Sales Rep';


Q2. display  last name, first name, and job title  of all employees who hold the job title 'Sales Rep', 

SELECT lastname, firstname, jobtitle
FROM employees
WHERE jobtitle = 'Sales Rep';



Q3. display  last name, first name, and job title, officeCode from employee who hold the job title 'Sales Rep' and officeCode is 1

SELECT lastname, firstname, jobtitle, officeCode
FROM employees
WHERE jobtitle = 'Sales Rep' AND officeCode = 1;


Q4. display  last name, first name, and job title, officeCode from employee who hold the job title 'Sales Rep' or officeCode is 1

SELECT lastname, firstname, jobtitle, officeCode
FROM employees
WHERE jobtitle = 'Sales Rep' OR officeCode = 1;


Q5. display  last name, first name, and job title, officeCode from employee  who's office code is >=1 and <=3

SELECT lastname, firstname, jobtitle, officeCode
FROM employees
WHERE officeCode BETWEEN 1 AND 3;

Q6. display  last name, first name, and job title, officeCode from employee  who's name ending with son

SELECT lastname, firstname, jobtitle, officeCode
FROM employees
WHERE lastname LIKE '%son';

Q7. display  last name, first name, and job title, officeCode from employee  who's office code any of (1,2,3)

SELECT lastname, firstname, jobtitle, officeCode
FROM employees
WHERE officeCode IN (1, 2, 3);

Q8. display  last name, first name, and reportTo from employee who is not reporting to anyone.

SELECT lastname, firstname, jobtitle, reportsTo
FROM employees
WHERE reportsTo IS NULL;

display unique lastname from employee

SELECT DISTINCT lastname
FROM employees;


Q9.display unique  state, city FROM customers WHERE state IS NOT NULL

SELECT DISTINCT state, city
FROM customers
WHERE state IS NOT NULL;


Q10. get the customers who are located in the USA or France,

SELECT *
FROM customers
WHERE country IN ('USA', 'France');


Q11. display customers who locate in the USA or France and have a credit limit greater than 100,000

SELECT *
FROM customers
WHERE country IN ('USA', 'France')
  AND creditLimit > 100000;


Q12. find the officecode, city , phone,country  that located in the U.S. or France,

SELECT officeCode, city, phone, country
FROM offices
WHERE country IN ('USA', 'France');


Q13. find employees whose first names start with  T , end with m, and contain any single character between e.g., Tom , Tim, you 
find employees whose first names start with a:

SELECT *
FROM employees
WHERE firstname LIKE 'T_m';


SELECT *
FROM employees
WHERE firstname LIKE 'a%';

Q14. search for employees whose last names don’t start with the character B

SELECT *
FROM employees
WHERE lastname NOT LIKE 'B%';


Q15. get the top five customers  with customerNumber, 
    customerName, 
    creditLimit
who have the highest creditLimit:

SELECT customerNumber, customerName, creditLimit
FROM customers
ORDER BY creditLimit DESC
LIMIT 5;


Q16. get the bottom five customers  who have the lowest creditsLimit:

SELECT customerNumber, customerName, creditLimit
FROM customers
ORDER BY creditLimit ASC
LIMIT 5;

find Nth highest salary in a table 
SELECT DISTINCT salary
FROM employees e1
WHERE N-1 = (
  SELECT COUNT(DISTINCT salary)
  FROM employees e2
  WHERE e2.salary > e1.salary
);

