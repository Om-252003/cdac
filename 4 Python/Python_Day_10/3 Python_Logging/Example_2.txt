import logging as lg

# Set up logging to a file
lg.basicConfig(filename='first.log', level=lg.INFO)

lg.debug('Debug message written to file')
lg.info('Info message written to file')
lg.error('error message written to file')
lg.warning('warning message written to file')
lg.critical('critical message written to file')
